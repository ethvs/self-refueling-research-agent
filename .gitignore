node_modules/
dist/
.env
.agent-state.json
.agent-orders.json
.claude/
# scratch reports from local mock runs
reports-mock/
# stray logs anywhere, but keep the per-report run logs that ship with the archived reports
*.log
!reports/*.log
